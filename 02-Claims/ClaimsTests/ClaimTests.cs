using System;
using System.Security.Claims;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using _02_Claims;

namespace ClaimsTests
{
    [TestClass]
    public class ClaimTests
    {/*
        [TestMethod]
        public void AddClaim_ShouldGetTrueBool()
        {
            Claim claim = new Claim();
            ClaimRepository repository = new ClaimRepository();

            bool addedcontent = repository.Add(claim);

            Assert.IsTrue(addedcontent);

        }

        private ClaimRepository _repo;
        private Claim _content;

        [TestInitialize]
        public void Arrange()
        {
            _repo = new ClaimRepository();
            string[] ingredients = { "vanilla", "ice", "baby", "too cold" };

            _repo.NewClaim(new Claim(1, "FOD", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(2, "asiojk", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(3, "fgeh5", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(6, "MANA", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(4, "Potion", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(5, "sword", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(6, "death", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(7, "Blade", "it's food you idiot", ingredients.ToList()));
            _repo.NewClaim(new Claim(8, "cats", "it's food you idiot", ingredients.ToList()));
        }

        [TestMethod]
        public void GetClaim_returnContent()
        {
            Claim searchResult = _repo.GetItemByName("FOD");
            Assert.AreEqual(searchResult.Name, "FOD");
        }

       */ 
    }
}
